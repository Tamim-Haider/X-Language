It is difficult to use multiple line so please embed multiple line code in one line using 'space'.

e.g. 
&welcome starts() { digit mark = 75; whatif(mark >= 80) { show("A+"); } otherwise(mark >= 50) { show("Average"); } ifnot { show("Failed"); }; } &end